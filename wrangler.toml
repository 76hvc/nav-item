name = "nav-item"
pages_build_output_dir = "web/dist"

[[d1_databases]]
binding = "DB"
database_name = "nav_db"
database_id = "your-d1-database-id" # 部署时需替换或在 Cloudflare 控制台绑定

# compatibility_flags = [ "nodejs_compat" ] # 移除此标志，已改用 Web Crypto 实现 JWT 以提高兼容性

[vars]
JWT_SECRET = "your_jwt_secret_key"
